/**
 * Compatibility re-export while engine boundary ownership lives in @generals/engine.
 */
export { GameLoop } from '@generals/engine';
export type { GameLoopCallbacks, GameLoopScheduler } from '@generals/engine';
